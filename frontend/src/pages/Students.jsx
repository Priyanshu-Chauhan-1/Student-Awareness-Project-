import { Card, Typography } from 'antd'

const { Title } = Typography

export default function Students() {
  return (
    <Card>
      <Title level={4}>Students</Title>
      {/* Next: table + create form */}
    </Card>
  )
}