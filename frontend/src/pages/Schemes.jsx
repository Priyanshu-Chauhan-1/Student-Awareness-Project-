import { Card, Typography } from 'antd'

const { Title } = Typography

export default function Schemes() {
  return (
    <Card>
      <Title level={4}>Schemes</Title>
      {/* Next: list schemes */}
    </Card>
  )
}